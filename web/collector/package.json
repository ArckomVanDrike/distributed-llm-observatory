{
  "name": "web-collector",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run generate:prompt-bank && vite",
    "build": "npm run generate:prompt-bank && tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "generate:prompt-bank": "node scripts/generate-public-prompt-bank.mjs"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.4.3",
    "vitest": "3.2.7"
  }
}
